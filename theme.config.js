/** @type {const} */
const themeColors = {
  // Paleta Gemini: Branco, Cinza Claro, Azul Suave
  primary: { light: '#1F2937', dark: '#F3F4F6' },       // Cinza escuro / Cinza claro
  background: { light: '#FFFFFF', dark: '#0F172A' },   // Branco / Azul escuro
  surface: { light: '#F9FAFB', dark: '#1E293B' },      // Cinza muito claro / Cinza escuro
  foreground: { light: '#111827', dark: '#F1F5F9' },   // Preto suave / Branco
  muted: { light: '#6B7280', dark: '#94A3B8' },        // Cinza médio
  border: { light: '#E5E7EB', dark: '#334155' },       // Borda cinza claro
  success: { light: '#059669', dark: '#10B981' },      // Verde sucesso
  warning: { light: '#D97706', dark: '#F59E0B' },      // Laranja aviso
  error: { light: '#DC2626', dark: '#EF4444' },        // Vermelho erro
  accent: { light: '#3B82F6', dark: '#60A5FA' },       // Azul suave
};

module.exports = { themeColors };
